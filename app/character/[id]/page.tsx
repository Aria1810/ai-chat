'use client';
/* eslint-disable react-hooks/set-state-in-effect */
import { useEffect, useState } from "react";
import { useParams, useRouter } from "next/navigation";
import { supabase } from "@/lib/supabase";
type Character = { id: string; name: string; avatar?: string; cover_url?: string; description?: string; story?: string; persona?: string; opening_message?: string; tags?: string[] };
type Comment = { id: string; content: string; created_at: string };
export default function CharacterDetail() { const { id } = useParams<{ id: string }>(); const router = useRouter(); const [character, setCharacter] = useState<Character | null>(null); const [comments, setComments] = useState<Comment[]>([]); const [content, setContent] = useState(""); const load = async () => { const [{ data: c }, { data: list }] = await Promise.all([supabase.from("characters").select("*").eq("id", id).single(), supabase.from("character_comments").select("*").eq("character_id", id).order("created_at", { ascending: false })]); setCharacter(c as Character | null); setComments((list ?? []) as Comment[]) }; useEffect(() => { if (id) load() }, [id]); const add = async () => { const { data: { user } } = await supabase.auth.getUser(); if (!user || !content.trim()) return; const { error } = await supabase.from("character_comments").insert({ character_id: id, user_id: user.id, content: content.trim() }); if (!error) { setContent(""); load(); } }; if (!character) return <main className="grid min-h-screen place-items-center bg-[#050508] text-white/50">正在加载角色…</main>; return <main className="min-h-screen bg-[#050508] text-white"><div className="h-64 bg-cover bg-center" style={{ backgroundImage: character.cover_url ? `linear-gradient(#05050855,#050508),url(${character.cover_url})` : undefined }} /><section className="mx-auto -mt-28 max-w-4xl p-6"><div className="rounded-[28px] border border-white/10 bg-[#09090d]/90 p-7 backdrop-blur"><div className="flex gap-6"><img src={character.avatar || "/placeholder.png"} alt="" className="h-28 w-28 rounded-2xl object-cover" /><div><h1 className="text-3xl font-black">{character.name}</h1><p className="mt-2 text-white/60">{character.description}</p><div className="mt-4 flex flex-wrap gap-2">{character.tags?.map(tag => <span key={tag} className="rounded-full bg-[#786BD4]/20 px-3 py-1 text-xs">#{tag}</span>)}</div><button onClick={() => router.push(`/chat/${id}`)} className="mt-5 rounded-xl bg-white px-5 py-2 font-bold text-black">开始聊天</button></div></div><div className="mt-7 grid gap-5 md:grid-cols-2"><article><h2 className="font-bold">角色背景</h2><p className="mt-2 whitespace-pre-wrap text-sm text-white/60">{character.story || "暂无背景介绍"}</p></article><article><h2 className="font-bold">性格设定</h2><p className="mt-2 whitespace-pre-wrap text-sm text-white/60">{character.persona || "暂无性格设定"}</p></article></div>{character.opening_message && <blockquote className="mt-6 border-l-2 border-[#a99cff] bg-white/[.03] p-4 text-white/75">“{character.opening_message}”</blockquote>}</div><section className="mt-6 rounded-[28px] border border-white/10 bg-white/[.025] p-7"><h2 className="text-xl font-bold">评论 ({comments.length})</h2><div className="mt-5 flex gap-3"><textarea value={content} maxLength={1000} onChange={e => setContent(e.target.value)} placeholder="写下你对这个角色的评价…" className="min-h-20 flex-1 rounded-xl border border-white/10 bg-black/25 p-3 outline-none" /><button onClick={add} className="h-11 rounded-xl bg-[#786BD4] px-5 font-bold">发布</button></div><div className="mt-6 space-y-4">{comments.map(comment => <article key={comment.id} className="border-t border-white/10 pt-4"><p className="text-sm font-bold">社区用户<span className="ml-3 font-normal text-white/35">{new Date(comment.created_at).toLocaleString()}</span></p><p className="mt-1 whitespace-pre-wrap text-sm text-white/70">{comment.content}</p></article>)}</div></section></section></main> }
